import { MediaContentBaseDirective } from './media-content-base.directive';

describe('MediaContentBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaContentBaseDirective();
    expect(directive).toBeTruthy();
  });
});
