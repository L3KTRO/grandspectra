services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grandspectra-frontend
    ports:
      - "3338:80"
    pull_policy: always

networks:
  app-network:
    driver: bridge

volumes:
  dbdata:
  laraveldata:
